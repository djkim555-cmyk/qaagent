# storyboard/solsol-brand 형식(템플릿) QA — 2차 재검증 (45개 전수)

> 정본: [`docs/storyboard/README.md`](../../storyboard/README.md). 1차 QA: [`_storyboard-qa-format.md`](_storyboard-qa-format.md)(5건 지적).
> 페르소나 = 형식/템플릿 준수만 검사(내용 진위는 다루지 않음). 이번 회차는 **1차 지적사항 5건이 실제로 고쳐졌는지 재확인** + **신규 분리 파일 `_pu02` 포함 45개 전체 재전수**.
> 대상: `docs/storyboard/solsol-brand/` 45개 = storyboard 문서 44개(1차 43개 + 신규 `S-BR01-0401-001_pu02-도메인-사용중-안내.md` 1개) + `EMAIL-TEMPLATES-전건미이관-요약.md`(템플릿 검사 예외, 폐기 안내 적절성만).
> 방법: 파일명 규칙(§1) · 헤더 1·2행(§2) · 5섹션 순서·제목(grep 전수) · 디자인 이슈 표 컬럼·심각도 enum(grep 전수 추출) · `[추정]`/`[미확정]` 표기·절대일자 · 내부 상대링크 실존(`ls`/`find` 실측) — 전부 셸 스크립트 기계 대조, 육안 대조 아님.
> 정리: 도담(QA) · 2026-07-27 (KST)

## 1차 지적사항 5건 재확인 결과

| # | 대상 파일 | 1차 지적 내용 | 재확인 방법 | 재확인 결과 | 판정 |
|---|---|---|---|---|---|
| 1 | `S-BR01-0702-001-문의-내역-목록.md` | 헤더 2행 상태 필드에 `상태: 작성완료 · **상태=중대(crit, blocker 미해소)**` 부가 텍스트 오염 | `sed -n '2p'` 로 헤더 2행 직접 열람 | `> route: /my/inquiry · 정리: 나래님 · 최종수정 2026-07-27 (KST) · 상태: 작성완료` — 부가 텍스트 완전 제거, 다른 43개와 동일 형식 | **고쳐짐** |
| 2 | `S-BR01-0401-001_pu01-도메인-중복확인-모달.md` 심각도 `정보`(정본 외 값) | 디자인 이슈 표 심각도 컬럼에 상/중/하 외 `정보` 값 사용 | 해당 섹션 전체 재출력 + 45개 전체 심각도 컬럼 grep 추출 | pu01 두 행 모두 `하`로 정정. **45개 전체에서 `정보` 값 0건**(신규 `_pu02` 포함) | **고쳐짐** |
| 2-부속 | `S-BR01-0401-001_pu02-도메인-사용중-안내.md`(신규 분리 파일) | 같은 문제 없는지 확인 대상 | 심각도 컬럼 추출 | 두 행 모두 `하`(순정값), pu01과 동일 패턴으로 정합. 헤더·5섹션·파일명 규칙도 전부 준수 | **문제 없음(신규 파일 클린)** |
| 3 | `S-BR01-0601-001-가격.md` / `S-BR01-0601-002-유료구매.md` 심각도 `상(blocker)` 괄호변형 | 심각도 컬럼에 괄호 주석 덧붙임 | 두 파일 디자인 이슈 섹션 재출력 | 0601-001: `상`/`중`(순정), 0601-002: `상`/`하`(순정). "blocker" 표시는 **이식 주의** 컬럼으로 이동(예: "…(blocker)") — 심각도 컬럼 자체는 정규화 완료 | **고쳐짐** |
| 4 | `S-BR01-0702-001` / `S-BR01-1001-001` 심각도 `**상**` 볼드변형 | 심각도 컬럼 값에 볼드 적용 | 두 파일 디자인 이슈 섹션 재출력 | 0702-001: `상`/`중`(볼드 없음, "blocker"는 **구분** 컬럼 `인터랙션 (**blocker**)`로 이동), 1001-001: `상`/`중`(볼드 없음, "blocker"는 구분 컬럼 `전역 인터랙션 (blocker)`로 이동). 심각도 컬럼 자체는 순정값 | **고쳐짐** |

**결론: 1차 지적 5건 전부 해소.** 45개 전체 심각도 컬럼 값을 grep으로 재추출한 결과 `상`/`중`/`하` 순정값만 존재(볼드·괄호주석·"정보" 등 변형 0건).

- 참고(결함은 아님, 관찰): "blocker" 개념을 표기하는 위치는 여전히 파일마다 다르다 — 0601-001/0601-002는 **이식 주의** 컬럼에, 0702-001/1001-001은 **구분** 컬럼에 병기. README 템플릿은 심각도 컬럼만 상/중/하 enum으로 못박고 구분·이식주의 컬럼 서식은 규정하지 않으므로 이는 **형식 위반이 아니다**(자유 텍스트 컬럼). 통일성 관점의 개선 여지로만 기록.

## 신규 발견 형식 문제

**없음.** 45개 전체(44 storyboard + EMAIL-TEMPLATES 예외 1) 기계 전수 대조 결과 신규 위반 0건.

세부 대조 항목별 결과:

| 검사 항목 | 대상 | 결과 |
|---|---|---|
| 파일명 규칙(`<화면ID>-<슬러그>.md`) + 헤더ID=파일명ID 일치 | 44개 | 위반 0건 |
| 헤더 1행 `# [<화면ID>] <화면명>` | 44개 | 위반 0건 |
| 헤더 2행 `> route: … · 정리: OO님 · 최종수정 YYYY-MM-DD (KST) · 상태: …` | 44개 | 위반 0건(정리자 이름=나래님/새롬님만 등장, 역할슬러그 없음) |
| 상태 필드 값(`작성완료`, 부가텍스트 없음) | 44개 | 위반 0건(1차 지적 1건 해소로 완전 통일) |
| 5섹션 순서·제목(기능 정의→표시 조건 · 상태·분기→정책→디자인 이슈→이식 주의 종합) | 44개 | 위반 0건 |
| 디자인 이슈 표 5컬럼 헤더(구분/이슈 내용/근거·영향/심각도/이식 주의) | 44개 | 위반 0건 |
| 디자인 이슈 심각도 enum(상/중/하 순정값만) | 표 있는 전체 행(35행) | 위반 0건(1차 5건 전부 정정 확인) |
| "이슈 없음" 케이스 = "특이사항 없음" 문구 처리 | 19개 파일 | 위반 0건 |
| `[추정]`/`[미확정]` 대괄호 마커 통일 | 전체 | 위반 0건(비마커 일반어휘 "~로 추정"·"추정치" 3건은 마커 아님, 오탐 아님 재확인) |
| 내부 상대링크 실존 | 링크 보유 3개 파일, 총 15개 링크(EMAIL-TEMPLATES 12 + 1001-001 3) | 끊어진 링크 0건 |

## 45개 중 완전 준수 요약

**44개 storyboard 문서(신규 `_pu02` 포함) 전부 형식 완전 준수.** EMAIL-TEMPLATES 요약 파일 1개는 템플릿 검사 대상 외(폐기 안내 문서) — 정리자·날짜·상태·링크 12건 실존 모두 적절.

- **완전 준수: 44/44**(storyboard 문서 기준). 1차에서 형식 편차가 있던 5건(0702-001·0401-001_pu01·0601-001·0601-002·1001-001)이 이번 재검증에서 전부 정정 확인됨.
- blocker 급 신규 결함: 0건.

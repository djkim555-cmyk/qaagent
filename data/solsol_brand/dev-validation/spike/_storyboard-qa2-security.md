# storyboard-qa2 보안/개인정보 페르소나 재검증

> 정리: 미쁨(security-reviewer) · 2026-07-27 (KST)
> 대상: `docs/storyboard/solsol-brand/` 보안민감 storyboard 28건 + 백엔드 코드 2건(`solsol-brand-api/src/routes/sites.ts`·`billing.ts`) 대조 + 1차 보안 페르소나 `_r04-persona-security.md` 일관성 대조

**판정**: 신규 blocker(상) **0건**. 실결제/실탈퇴 미연동(전부 mock·TODO) 상태라 실질 위험은 전반적으로 낮음. 다만 **문서가 클라 전용 방어를 안전장치로 서술(서버 미강제)** 하는 과소평가 1건이 실연동 전제조건으로 처리돼야 함.

## 발견 표

| 파일 | 이슈 | 근거 | 심각도 | 권고 |
|---|---|---|---|---|
| `_pu02·_pu03·_pu04`(카드 삭제/대표/최소1장) | "대표카드 삭제불가"·"최소 1장 유지" 안전장치가 **클라이언트 전용**으로 서술됨. 백엔드 `DELETE /api/billing/cards/:id`는 대표카드/최소1장 검증 없이 soft-delete(대표 삭제 시 `isDefault=0`으로 밀어냄) — 클라 우회 시 활성 구독의 유일/대표 카드 삭제 가능(코드 주석도 "활성 구독 참조 시 결제 실패 위험" 인정) | `billing.ts:249-276`(default/count 가드 없음, 소유권 체크는 있음 → IDOR는 방어됨) ↔ `_pu04` "클라에서 `card.isDefault` 먼저 판별" | **중** | 세 문서에 "이 불변식은 서버측 강제 필요(현재 클라 전용)" flag 추가 + brand-api DELETE에 대표/최소1장 서버검증을 **실결제 오픈 전제조건**으로 원장 등재 |
| `1201-001~012`(이메일 템플릿) | "오너 확인(외부 발신)" 표기 불일치 — 001·002·003만 태그, 004~012는 동일 외부 발신 트랜잭션 메일인데 미태그(또는 001-003 과다) | 12파일 대조(예: `1201-003`은 태그有, `1201-004`·`006`·`007`·`010`은 무) | **하** | 외부 발신을 오너 확인 대상으로 볼지 기준 확정 후 12종 전건 통일(현재 실 발송 전부 TODO라 실질 위험 낮음) |
| `0301-001-회원가입`·`0302-003`(D11) | D11 해소 커밋 귀속이 1차 페르소나와 상충: persona=`closed@71f948d`, storyboard=`closed@0a50429`(+"71f948d 오귀속 정정") | `_r04-persona-security.md:13,29` ↔ 두 storyboard 정책절 | **하(정보성)** | 보안 결론(규칙 통일·서버 `isPasswordValid` 강제)은 양쪽 동일 — 커밋 provenance만 원장 정합 확인 |
| `0401-001_pu01/pu02`(도메인) | 문서 표기 "슬러그 3~30자" vs 백엔드 에러문구 "3~50자" | `sites.ts:255` ↔ storyboard 정책절 | **하(콘텐츠, 보안 아님)** | 실 `SLUG_PATTERN` 기준으로 표기 정정 |

## 오너 확인 표기 — 누락/과다

- **적절(핵심 위험 화면 커버 확인)**: 계정삭제 `_pu05`(탈퇴/비가역)·카드등록 `0601-007`(금전/결제)·구독취소 `0801-003`(accepted-risk 오너승인 2026-07-16 명시)·결제이메일변경 `0901-002`(계정/보안).
- **과다 후보**: 이메일 `1201-001~003`의 "외부 발신" 태그(위 표 2번과 연동 — 나머지 9종과 기준 불일치가 본질).
- **누락 아님으로 판정**: 회원가입 `0301-001`의 필수 약관 동의(README 오너확인 목록의 "약관"은 약관 변경/공개를 지칭할 개연성이 크고 사용자 동의 자체엔 부적용) · 새 비밀번호 `0302-003`(비가역/금전 아님).

## PII 노출 점검 — 위반 없음(통과)

- 이메일 템플릿 12종 전수: 실명·실 이메일주소·실 카드번호·실 결제정보 미포함. `{{USER_NAME}}` 플레이스홀더, 더미 인증코드(`357690`), 더미 토큰만 사용.
- 카드 관리 문서: 마스킹 규약 `{issuer}(****{끝4자리})` 준수, 카드등록은 뒤 4자리만 입력(전체 PAN 미보관).

## check-slug API 정보노출 점검 — 위반 없음(통과)

- `sites.ts:245-266`: `{ available: boolean }`만 반환, 내부 select는 `id`만이라 응답 미노출. drizzle `eq()` 파라미터화로 SQLi 없음, `SLUG_PATTERN` 선검증. slug 존재여부는 공개 서브도메인 특성상 정보노출 아님.

## D10 재검토

오픈리다이렉트 서술(로그인 `0302-001`)이 1차 페르소나와 완전 일관, 위험 과소·과대평가 없음.

## 최종 요약

- 검토 파일수: storyboard 28건 + 백엔드 코드 2건 + 1차 페르소나 1건 대조
- 발견 4건(상 0·중 1·하 3), PII·check-slug 통과 확인 2건
- **핵심**: 카드 대표/최소1장 안전장치가 서버 미강제(중) — 실결제 연동 착수 시 blocker로 승격 필요. 그 외는 표기 불일치 수준(하).

deployer 게이트 관점: 문서 산출물이며 실연동 아니므로 보안 축 NO-GO 사유 없음. 실결제 연동 착수 시 발견 1번은 blocker로 재평가 필요.

# storyboard/solsol-brand 형식(템플릿) QA — 전수 검토

> 정본: [`docs/storyboard/README.md`](../../storyboard/README.md). 페르소나 = 형식/템플릿 준수만 검사(내용 진위는 다루지 않음).
> 대상: `docs/storyboard/solsol-brand/` 44개(`EMAIL-TEMPLATES-전건미이관-요약.md` 1개는 지시대로 템플릿 검사 제외, 폐기 안내 적절성만 확인) — 43개 storyboard 문서 전수 기계 대조.
> 방법: 파일명 규칙(README §1) · 헤더(§2) · 5섹션 순서·제목(§2, grep 전수 대조) · 디자인 이슈 표 컬럼·심각도 enum(§2) · `[추정]`/`[미확정]` 표기·절대일자(§3) · 내부 상대링크 실존 여부(`ls`/`find` 실측) 기계 대조.
> 정리: 도담(QA) · 2026-07-27 (KST)

## 결함 표

| 파일명 | 문제유형 | 상세 | 심각도 |
|---|---|---|---|
| `S-BR01-0702-001-문의-내역-목록.md` | 헤더형식 | 2행 상태 필드에 README §2 템플릿(`상태: 작성중/작성완료`)에 없는 텍스트 부착 — 실제: `상태: 작성완료 · **상태=중대(crit, blocker 미해소)**`. `상태:` 키가 문서 안에 2번 등장하는 모양이 되어, 향후 상태 필드를 기계 파싱하는 도구가 있으면 오탐 소지. 44개 중 이 파일만 유일하게 헤더 라인에 부가 텍스트를 붙임(다른 43개는 전부 `상태: 작성완료`로 종결) | 하 |
| `S-BR01-0401-001_pu01-도메인-중복확인-모달.md` | 표기규칙(심각도 enum) | 디자인 이슈 표의 "심각도" 컬럼에 README/CLAUDE.md가 정의하지 않은 **`정보`** 값 2행 사용(정본 심각도 스케일은 상/중/하 3단계 단일 — README §2 템플릿 주석 "상/중/하", CLAUDE.md "심각도 상/중/하 단일"). 44개 문서 전체를 통틀어 "정보" 값은 이 파일에만 등장 | 중 |
| `S-BR01-0601-001-가격.md` | 표기규칙(심각도 enum) | 심각도 컬럼 값이 `상(blocker)`로, 표준 enum(상/중/하) 값에 괄호 주석을 덧붙인 변형 표기 — 다른 blocker 사례(`S-BR01-0702-001`·`S-BR01-1001-001`)는 심각도 컬럼을 `**상**`(순정 값 + 볼드)로 두고 "blocker"는 별도로 구분(`구분` 컬럼 또는 `이식 주의` 컬럼)에 적음. 즉 같은 "blocker" 개념을 파일마다 다른 위치·다른 표기로 기록해 3가지 변형 공존 | 하 |
| `S-BR01-0601-002-유료구매.md` | 표기규칙(심각도 enum) | 위와 동일 — 심각도 컬럼 `상(blocker)` | 하 |
| `S-BR01-0702-001-문의-내역-목록.md` / `S-BR01-1001-001-결제-내역.md` | 표기규칙(강조 불일치) | 심각도 컬럼 값에 `**상**`(볼드)을 적용 — 나머지 파일들은 심각도 값을 볼드 없이 일반 텍스트(`하`·`중`·`상`)로 표기. 같은 "심각도=상(blocker)" 상황을 표현하는 3파일(0601-001·0601-002·0702-001·1001-001)이 서로 다른 3가지 방식(괄호주석/볼드/구분컬럼 표기)을 씀 | 하 |

## 문제 없는 항목 요약

- **파일명 규칙**(README §1): 43개 전부 `<화면ID>-<화면명 슬러그>.md` 형식 준수, 헤더의 화면ID와 파일명 선두 화면ID 100% 일치, 파일명만으로 화면ID 식별 가능. 위반 0건.
- **헤더 1행**(`# [<화면ID>] <화면명>`): 43개 전부 정확히 준수. 위반 0건.
- **헤더 2행**(`> route: ... · 정리: <이름>님 · 최종수정 YYYY-MM-DD (KST) · 상태: ...`): "정리: OO님" 표기(역할 슬러그·이름만 표기 금지) 43개 전부 준수, "최종수정" 날짜 43개 전부 `2026-07-27 (KST)` 절대일자(상대날짜 없음). 상태 필드 자체 값(`작성완료`)도 43개 전부 동일 — 단 `S-BR01-0702-001` 1건만 추가 텍스트 부착(위 표 참조).
- **5섹션 순서·제목**(`## 기능 정의` → `## 표시 조건 · 상태·분기` → `## 정책 (변경 사항 · 값·수치)` → `## 디자인 이슈` → `## 이식 주의 종합`): grep 전수 대조 결과 43개 **전부** 순서·제목 오탈자 없이 정확히 일치. 위반 0건(가장 엄격한 규칙인데 완전 준수).
- **디자인 이슈 표 컬럼**(`구분 | 이슈 내용 | 근거/영향 | 심각도 | 이식 주의`): 43개 전부 정확한 5컬럼 헤더·구분선 사용. "이슈 없음" 케이스(12종 이메일 파일 전체 + `S-BR01-0701-001_pu01`·`S-BR01-0801-003`·`S-BR01-0901-001_pu01~pu05` 등 14개)는 전부 "특이사항 없음"(또는 사유 병기)으로 명시 — README 규칙대로 표 생략하고 문장 처리. 위반 0건.
- **`[추정]`/`[미확정]` 대괄호 표기**: 43개 문서에서 등장한 모든 `[추정]`·`[미확정]` 마커는 예외 없이 대괄호로 통일. (`추정치`·`~로 추정` 같은 서술어 3건은 마커가 아닌 일반 어휘 사용이라 표기규칙 대상 아님 — 오탐 아님 확인.)
- **내부 상대링크**: 상대경로 링크를 포함한 파일은 2개뿐(`EMAIL-TEMPLATES-전건미이관-요약.md`, `S-BR01-1001-001-결제-내역.md`) — 총 15개 링크 전부 `ls`/`find`로 실존 확인, 끊어진 링크 0건.
- **`EMAIL-TEMPLATES-전건미이관-요약.md`(템플릿 예외, 폐기 안내 적절성만 확인)**: 2026-07-15 결정이 2026-07-27 오너 재승인으로 번복돼 12종 화면ID가 개별 배정됐다는 경위, 근거 문서 링크, 개별 storyboard 12건 링크(전부 실존 확인), 정리자·날짜·상태(`폐기(개별 파일 참조)`) 모두 명시 — 폐기 안내로서 적절.

**전체 44개 중 39개 형식 완전 준수**(개별 파일 5개에 경미~중간 수준 형식 편차, blocker급 없음).

## 가장 흔한 문제 유형 Top 3

1. **심각도(severity) enum 표기 불일치** — `상/중/하` 3단계 정본 외에 `정보`(신규 4번째 값)·`상(blocker)`(괄호주석)·`**상**`(볼드) 3가지 변형이 파일마다 혼재. 나래님·새롬님 작업분 구분 없이 여러 파일에 걸쳐 반복되는 유일한 패턴형 문제.
2. **"blocker" 표기 위치 비표준화** — blocker임을 나타내는 방법이 파일마다 다름(심각도 컬럼에 괄호 병기 / 구분 컬럼에 병기 / 심각도 값 볼드). 5개 결함 표 항목 모두 이 문제로 수렴.
3. **헤더 라인 오염(단발성)** — `S-BR01-0702-001` 1건에서만 상태 필드에 자유 텍스트 추가. 나머지 42개는 완벽히 표준 준수라 이 건이 유일한 예외.

# BR01-r05 — 스토리보드 MD ↔ 실 구현 코드 정합성 검증

- **상태**: Track A(30건, 화면ID 기준 32건) 검증 완료
- **최종수정**: 2026-07-28
- **라운드 성격**: `docs/storyboard/solsol-brand/`(44개 화면, 2026-07-27 신설 체계)와 실제 `solsol-brand` 앱 코드를 4축(기능정의·표시조건/상태분기·정책·문구)으로 1:1 대조. br01-r02~r04는 "목업2 ↔ 스파이크" 대조였고, 이번 r05는 "스토리보드 문서 ↔ 스파이크" 대조로 성격이 다르다(정본 축 전환).
- **계획**: `docs/architecture/BRAND_FRONT_SPIKE_PLAN.md` §8.1
- **검증 주체**: 바다(frontend, 5개 배치 B1~B5 병렬), 가온(총괄, 오케스트레이션·교차확인)
- **개발 주체**: 이전 세션(나래·바탕, W0~W4) — 본 라운드와 분리돼 게이트 효력 있음

## 0. 요약 집계

| 구분 | 건수 |
|---|--:|
| 일치(GO) | 27 |
| 부분 불일치(중, 확인 필요) | 1 |
| 실질 불일치(상, 문서 재작성 필요) | 1 |
| 문서 명확성/갭(하) | 2 |
| 신규 storyboard 작성(B5, 별도) | 5 |
| **검증 대상 합계** | **32** |
| 기존 결함 재확인(신규 아님, blocker 원장 기 등재) | 4건(D01·D06·D09·D10 계열, 접근성·실시간검색 등) |

## 1. 신규 발견 — 확인 필요(오너/기획-lead)

| ID | 화면ID | 대조 항목 | 판정 | 심각도 | 현상 | 근거 |
|---|---|---|---|---|---|---|
| r05-D01 | S-BR01-0401-002 | 정책 | 부분불일치 | 중 | 사이트 목록에서 사이트명 클릭 시 storyboard는 "관리자단 새창 연결"(목업2 원본과 동일, `admin.solsol.com`)이라 서술하나, 실제 코드(`my/sites/index.vue:145`)는 사이트 자체(크리에이터) 공개 도메인으로 연결됨. `admin.` 문자열 자체가 앱 코드에 0건. | `app/pages/my/sites/index.vue:145` ↔ `S-BR01-0401-002-사이트-목록.md` |
| r05-D02 | S-BR01-0702-002 | 기능정의 전체 | 실질불일치 | **상** | storyboard가 서술하는 댓글 인라인수정·삭제·대댓글(중첩)·페이지네이션·소프트삭제가 코드에 전혀 없음(신규 답변 등록만 가능). 원인은 구현 누락이 아니라 백엔드 계약 제약(`TB_CONTACT_REPLY`에 부모답글ID 없어 중첩 스레드 자체가 불가 — 오늘 스파이크 가능성 검토에서 나래(dba)가 이미 지적한 사안과 동일 근본원인). 조치는 코드 수정이 아니라 **문서를 계약 현실에 맞게 재작성**하는 쪽. | `app/pages/my/inquiry-detail.vue:6-8,33-44,190-239` ↔ `S-BR01-0702-002-*.md` |

## 2. 문서 개선(하, 즉시 반영 가능)

| ID | 화면ID | 내용 |
|---|---|---|
| r05-D03 | S-BR01-0302-003 | "마크업 표기는 영문 대소문자" 서술이 실제로는 앱 코드가 아니라 목업2 원본 HTML(`mockup2/.../password-reset-new.html:35`)을 가리킴 — "목업2 마크업 표기"로 명확화 권고. 기능적 불일치 아님(앱 코드·검증 로직은 이미 일치). |
| r05-D04 | S-BR01-0701-001 | storyboard 기능정의가 문의유형별(상품/결제/제휴/서비스/기타) 프리필 콘텐츠 렌더링 로직(`inquiry.vue:21-127`, 코드 절반 이상)을 서술하지 않음 — 보완 필요. 참조 정책문서(`docs/policy/brand/front/inquiry.md`)도 동일 갭. |

## 3. 기존 결함 재확인(신규 아님, 원장 기 등재)

| 화면ID | 내용 | 상태 |
|---|---|---|
| S-BR01-0601-001 | 가격 페이지 "구매하기" CTA가 `/purchase`가 아니라 `/login?next=/my/sites`로 오배선. 앱 전체에서 `/purchase`로 링크하는 코드 0건, 사실상 고아 페이지. | **미해소**(blocker, 상) |
| S-BR01-1001-001 | 결제내역 검색이 엔터/돋보기 클릭 트리거 규칙 위반(실시간 필터), 상태필터 키보드 접근 불가. | **미해소**(blocker, 상) |
| S-BR01-0702-001 | 문의내역 목록도 동일 실시간필터 패턴, 케밥·excerpt·답변수 아이콘 미이식. | **미해소**(상/중) |
| S-BR01-0901-001_pu01~05 | 계정관리 모달 5종 포커스트랩·복귀 로직 부재. | **미해소**(중, 접근성) |
| S-BR01-9001-001/002/004/005 | 에러화면 4개 파일 코드 주석의 화면ID가 짝과 뒤바뀜(예: `error.vue:3`이 "404(9001-001)"로 오기, 정본은 404=9001-002). 표시 내용·분기 로직엔 영향 없음. | **미해소**(하) |

## 4. 신규 작성 — 약관류 5화면 경량 storyboard (B5)

오너 07-28 승인. `docs/storyboard/solsol-brand/S-BR01-1101-001~005-*.md` 5개 파일 신규 작성 완료.

| 화면ID | 상태 |
|---|---|
| S-BR01-1101-001(이용약관) | ✅ 1차가안 반영(2026-07-28) |
| S-BR01-1101-002(무료약정상품 이용약관) | ⚠️ 구 스텁, 1차가안 미반영 |
| S-BR01-1101-003(유료약정상품 이용약관) | ⚠️ 구 스텁, 구 용어체계(terms.vue 신규 용어체계와 불일치 가능) |
| S-BR01-1101-004(개인정보처리방침) | ✅ 1차가안 반영(2026-07-28) |
| S-BR01-1101-005(마케팅 정보 수신 동의) | ⚠️ 구 스텁, 1차가안 미반영 |

## 5. 전건 일치 확인(별도 조치 불요)

S-BR01-0301-001·0301-002_pu01·0302-001·0401-001_pu01·0401-001_pu02·0601-002·0601-004·0601-007·0701-001_pu01·0801-001·0801-002·0801-003(+_pu01)·0901-001(부모, policy 대체 검증)·9001-002(404, `app/error.vue`) — 총 17건(화면ID 기준), 4축 전부 일치. 근거는 각 배치 원본 산출물(에이전트 트랜스크립트, 세션 내 보관) 참조.

## 5-1. 배포 후 오너 제보 — 실제 런타임 버그 발견·즉시 수정 (2026-07-28)

| ID | 화면ID | 내용 | 조치 |
|---|---|---|---|
| r05-D05 | S-BR01-0702-001 | `/my/inquiry`(문의 내역 목록) 콘텐츠 영역이 완전히 공란으로 렌더되는 실제 런타임 버그. 원인: `solsol-brand-api`의 `GET /api/contact`가 목록을 배열째로 반환(`okWithMeta(c, items, …)`)하는데 프론트(`my/inquiry.vue:33-35`)는 `{ items: [...] }` 래핑을 기대 — `data.items`가 `undefined`가 되어 `.length` 접근 시 `TypeError`, Vue 렌더가 통째로 실패. `invoices.ts`·`payments.ts`는 이미 `{ invoices/payments: [...] }` 래핑 관례를 따르는데 `contact.ts`만 예외였음. | **수정 완료** — `contact.ts` 목록 응답을 `{ items }`로 래핑, typecheck 통과 후 배포(`c0a0954`). Playwright로 재현→수정→재확인(빈 상태 "문의 내역이 하나도 없네요!" 정상 렌더) 완료. `news.ts`도 동일 패턴이나 프론트 소비자가 아직 없어 미수정(활성 버그 아님, 참고만). |

Track A 검증 당시(B3, r05-D02 이전) `my/inquiry.vue`는 "일치"로 판정됐었다 — 이는 코드-문서 정적 대조로는 잡히지 않는 **런타임 계약 버그**였고, 실제 브라우저 조작(Playwright)으로만 발견됐다. 이후 라운드에 실제 브라우저 검증을 보강 필요.

## 5-2. 전 화면 API 연동 전수 검수 (2026-07-28, 오너 지시)

38개 화면(`app/pages/**/*.vue`) 전건을 5개 배치(api-developer 4·frontend-developer 1) 병렬 정적 계약대조 + 가온(총괄) Playwright 실브라우저 검증으로 확인. 정적 대조 기준: 프론트가 기대하는 응답 타입 vs 백엔드(`solsol-brand-api`) `ok`/`okWithMeta`/`created` 실제 반환 형태 1:1 일치 여부.

| 배치 | 화면 수 | 결과 |
|---|---:|---|
| G1 인증/계정 | 9 | 전부 일치 |
| G2 사이트+가격결제 | 8 | 🔴 2건 발견(아래) |
| G3 이용상품+결제내역 | 5 | 전부 일치 |
| G4 문의 | 4 | 전부 일치(오늘 수정된 `my/inquiry.vue` 재확인 포함) |
| G5 정적/공개 | 12 | API 없음(정적), 문제 없음 |

**Playwright 실측으로만 발견된 추가 결함(정적 대조로는 안 잡힘)**:

| ID | 화면ID | 내용 | 조치 |
|---|---|---|---|
| r05-D06 | S-BR01-0901-002/003 | `/my/account/billing-email` 접속 시 `billing-email.vue`가 아니라 부모 `my/account.vue` 내용이 그대로 렌더됨. 원인: Nuxt 파일기반 라우팅에서 `my/account.vue`(리프)와 `my/account/billing-email.vue`(자식)가 공존 — `/my/sites` 라우팅 충돌(커밋 `0978329`)과 동일 패턴. | **수정 완료** — `git mv my/account.vue my/account/index.vue`(커밋 `e3ac365`), typecheck·Playwright 재확인(제목 "계정 관리"→"결제 이메일 변경" 정상 분리) 완료. |
| r05-D07 | S-BR01-0601-002 | `purchase.vue`의 `paidPlans` 필터가 `code!=='free'`만 걸러 enterprise(가격 협의제, `monthlyPrice`/`yearlyPrice` DB null)를 포함 — enterprise 탭 선택 시 `null.toLocaleString()`으로 화면 전체 크래시 가능(오늘 발견한 contact.ts 유형과 동일 계열: 타입 계약과 실제 null 가능성 불일치). `pricing.vue`는 `CARD_ORDER`가 enterprise를 이미 배제해 안전했음. | **수정 완료** — `pricing.vue`와 동일하게 `enterprise` 제외(커밋 `5604493`), typecheck 통과. |
| r05-D08 | S-BR01-0401-002 | `my/sites/index.vue`가 `usage.storageLimit`/`storageUsed`(BIGINT bytes)를 단위 변환 없이 `"${값}GB"`로 그대로 출력 — 예: 200GB 플랜이 "214748364800GB"로 표시. `pricing.vue`는 이미 bytes→GB/TB 변환 함수(`formatByteUnit`)가 있었으나 이 화면엔 없었음. | **수정 완료** — `pricing.vue`와 동일한 변환 로직 이식(커밋 `5604493`), typecheck 통과. |

배포: solsol-brand 커밋 `e3ac365`(라우팅)·`5604493`(enterprise+저장용량) → 배포(`a6587eff.solsol-brand.pages.dev`). Playwright로 라우팅 수정 재확인 완료(billing-email 페이지 타이틀 정상 분리).

**결론**: 정적 코드-계약 대조만으로는 라우팅 충돌(r05-D06)을 잡지 못했다 — 실제 브라우저 네비게이션으로만 발견 가능했다. 반면 enterprise 크래시(r05-D07)·저장용량 단위(r05-D08)는 정적 대조(타입-null 가능성 교차확인, 단위 변환 로직 유무 확인)로 발견됐다. **두 방법 모두 필요**하다는 게 이번 전수검수의 핵심 교훈.

## 6. 다음 단계

- **r05-D01**(사이트명 링크 대상): 의도된 사양 변경인지 놓친 결함인지 오너 확인 대기.
- **r05-D02**(문의상세 댓글 CRUD): storyboard 재작성 방식·시점 확인 대기.
- Track B(개발 11건)는 이 라운드와 별도로 진행 — billing-email API+화면(`S-BR01-0901-002/003`) 구현 완료, 메일 발송 스텁+트리거 10건 배선 완료, 보안검토 1건(HIGH, resetPath 토큰 로그노출) 발견·수정·재검증 완료. 상세는 팀 활동내역 참조.

# BR01-r02 — 브랜드 사용자단 스파이크 검증 결함표

> **라운드**: br01-r02 (영역 전역 단조증가, r01=목업). **성격**: 스파이크(경량 PoC) + 실연동(P1 조회·P2 인증/PII) → security·privacy 서명 대상.
> **대상**: 배포 https://solsol-brand.pages.dev (실 brand-api+Aurora·BFF-lite·시드 셀러) · 코드 `solsol-brand/{app,server}` · 계약 `solsol-brand-api/src` · 스키마 `docs/data-model`.
> **검증(허브 독립)**: 도담(qa)·미쁨(security)·가림(privacy)·바다(data 5축). 2026-07-10.
> **정적검증**: `pnpm build` EXIT 0 · `vue-tsc --noEmit` EXIT 0 · `lint` EXIT 0(경고 1=D08).

## 종합 판정 — **조건부 GO**

스파이크 실연동 목표(프론트↔brand-api↔실 Aurora, 로그인 E2E) **달성**. **security·privacy 조건부 서명 확보(blocker 0)**. 단 **현 스파이크 실사용 차단 blocker 2건(D01·D02) 시정 필요**. 나머지(커버리지 갭·GA 조건·스키마 드리프트)는 후속 라운드 이월.

| 렌즈 | 서명/판정 |
|---|---|
| security(미쁨) | **조건부 GRANTED** (blocker 0) — GA 전 D09~D11 시정 조건 |
| privacy(가림) | **조건부 GRANTED** — D13은 프론트 미호출이라 현 스파이크 미차단(실 탈퇴 오픈 전 조건) |
| data(바다) | D03 blocker(advanced 플랜 시드 전 필수·현 스파이크는 TB_PLAN 미시드라 미차단) |
| qa(도담) | **blocker D01·D02**(현 스파이크 실동작 차단) |

## 결함표

| 결함ID | 추적키 | 축 | 심각도 | 현상(재현) | 기대 vs 실제 | 근거(파일:라인) | 담당 | 상태 |
|---|---|---|---|---|---|---|---|---|
| br01-r02-D01 | /my/**·/purchase*·/inquiry* | 인증게이트 | **상** | 로그인 후 하드리로드/직접URL로 `/my/sites` 진입 → 302 `/login`. 세션쿠키 `Path=/api`라 `/my/**` 요청에 미포함→SSR 게이트가 무세션 판정("새로고침=로그아웃") | 유효세션이면 새로고침/딥링크로도 보호화면 유지 / 매번 로그인 튕김 | `server/utils/session.ts:73`(path:'/api')·`app/middleware/01.require-auth.global.ts:20-30` | 나래·바탕 | **closed@r02-fix** (쿠키 Path=/·httpOnly/SameSite 유지, 커밋 `c4bcb63`. 프로덕션 재검증: 실 시드셀러 세션으로 /my/sites 직접 GET **200**·비로그인 302) |
| br01-r02-D02 | S-BR01-0801-* | 회귀·플로우 | **상** | `my/sites.vue` "플랜업그레이드·구독취소·사이트만들기" 버튼 전부 disabled 고정 → W4 완성 화면(product-cancel/upgrade/pay-now/auto-renew)이 UI서 도달 불가(오펀) | W4 후 사이트 카드 액션이 게이트 화면 연결 / 직접URL만 가능 | `app/pages/my/sites.vue:90,133,134,155` | 나래 | **closed@r02-fix** (버튼→W4 연결·subscriptionId/siteId 전달, GET /api/subscriptions 매핑, 커밋 `c4bcb63`. 사이트만들기(D05)는 미구현이라 disabled 유지) |
| br01-r02-D03 | TB_PLAN.storage_limit | 데이터·스키마 | **상** | 라이브/ORM 타입 `DECIMAL(18,6)`(≤~931GiB) — advanced 1TB(bytes 13자리) INSERT 시 범위초과 실패. docs는 BIGINT(정정본). **advanced 플랜 시드 전 필수 선행** | docs BIGINT ↔ 라이브 DECIMAL | docs `master.sql:156` vs `solsol-api/db/migrations/000_master.sql:143`·`schema.master.ts:186` | 바다·바탕 | **closed@migration**(타입 BIGINT로 라이브 Aurora 적용·검증. 마이그 `006_master_brand_r2.sql`. ⚠️단 기존 free 행 `storage_limit=5`(구 GB의미) 값 미변환 → 값 정정 DML 별건) |
| br01-r02-D04 | 1101-001~005·9001-001~005 | 커버리지·링크 | 중 | 약관5·에러5 화면 미구현 → footer·purchase 등에서 `/terms-free`·`/privacy` 링크 **404** | 정적 약관/에러 존재(계획 §2) / 전부 미구현·깨진 링크 | 라이브 curl 404·`AppFooter.vue`·`purchase.vue:271` 등 | 나래 | **closed@bd1f51a**(약관5 `terms.vue`·`terms-free.vue`·`terms-paid.vue`·`privacy.vue`·`marketing-consent.vue` + 에러5 `error.vue`(404/500 분기)+`error/{system,network,maintenance-emergency,maintenance-scheduled}.vue` 신설, footer/purchase 링크 200 해소. 허브 독립검증(qa 도담) 2026-07-15: solsol-brand HEAD(5e7ced2, bd1f51a 후행)에 12개 파일 전건 실존 확인) |
| br01-r02-D05 | S-BR01-0401-001 | 커버리지 | 중 | 내 사이트 만들기 화면·라우트 부재 → 신규 셀러 사이트 생성 경로 전무(my/sites 항상 빈상태) | 최소1 사이트 생성 가능 / 생성 경로 없음 | `find app/pages`·`my/sites.vue:90` | 나래·강기획 | **closed@bd1f51a**(`app/pages/my/sites/create.vue` 신설 — `/my/sites/create`, POST /api/sites·도메인중복 MOCK, `sites.vue` 버튼 연결. 허브 독립검증(qa 도담) 2026-07-15: 파일 실존·HEAD 후행 확인) |
| br01-r02-D06 | S-BR01-0901-002 | 커버리지 | 중 | 결제 이메일 변경 화면·라우트 부재(계정 내 안내만). 계획은 MOCK 처리였지 화면부재 아님 | 최소 MOCK 스텁 화면 / 화면 0 | `app/pages/my/account.vue` | 나래 | **closed@bd1f51a**(`app/pages/my/account/billing-email.vue` 신설 — `/my/account/billing-email`, 인증코드 실연동·저장 EP부재 MOCK, `account.vue` 진입 연결. 허브 독립검증(qa 도담) 2026-07-15: 파일 실존·HEAD 후행 확인) |
| br01-r02-D07 | S-BR01-0702-002 | CRUD·계약 | 중 | 문의 삭제/수정 EP(PATCH/DELETE /api/contact/:id) 부재 → U·D는 MOCK. 계획 §5 미비계약 4건 표에 미등재(5번째) | P2 종료조건 "문의 CRUD 왕복" / C·R만 실동작 | `app/pages/my/inquiry-detail.vue:32-42`·계획 §5 | 바탕 | open |
| br01-r02-D08 | 공용 ApiStateNotice | 보안·회귀 | 중 | `ApiStateNotice.vue:36` `desc`를 v-html 렌더, `error.message`(업스트림) 바인딩 → XSS 방어심도 결여(lint 경고 1) | 업스트림 문자열 escape / v-html 통과 | `app/components/ui/ApiStateNotice.vue:36` | 나래 | open |
| br01-r02-D09 | 인증/세션 | 보안 | 중 | refresh stateless(원장 없음) — 회전 시 구토큰 무효화·재사용탐지 없음, 로그아웃해도 refresh JWT TTL(14일) 유효 | 회전/로그아웃 시 즉시 폐기 / 만료까지 재사용 가능 | `solsol-brand-api/src/routes/auth.ts:440-502`(SEC-3) | 바탕·바다 | open(GA 조건) |
| br01-r02-D10 | /auth/* | 보안·남용 | 중 | 로그인·email-code/issue·reset-request 레이트리밋 전무(라이브 3연타 200) | 브루트포스·발송남용 차단 / 무제한 | `solsol-brand-api/src/{index.ts,routes/auth.ts}` rate-limit grep 0 | 바탕 | open(GA 조건) |
| br01-r02-D11 | BFF 프록시 | 보안·감사 | 중 | BFF가 실 클라 IP 미전달 → TB_LOGIN_LOG.ip=BFF egress, D10 IP리밋 시 전 사용자 단일IP 오인 | 원 클라IP 관측 / BFF IP만 | `server/utils/brandApi.ts:58-62`·`auth.ts:384` | 바탕 | open(GA 조건) |
| br01-r02-D12 | signup | 보안·정책 | 하 | signup이 A6 verifyToken 미강제 → 이메일 소유증빙 없이 가입 레코드 생성 가능 | 이메일 인증 후 가입 / 미검증 가입 | `solsol-brand-api/src/routes/auth.ts:263-358` | 바탕·가림 | open(정책) |
| br01-r02-D13 | 계정탈퇴 | privacy | 상 | 탈퇴 모달 "영구삭제" vs 백엔드 소프트삭제(status=-1)+파기배치 없음. 개인정보방침 "지체없이 파기"와 상충. **단 프론트 미호출(확인게이트까지)** | 문구=처리=정책 일치 / 상충 | `AccountDeleteModal.vue:71-84`·`account.ts:284-323`·`signup.vue:466` | 오너·기획·바탕 | open(실 탈퇴 오픈 전) |
| br01-r02-D14 | 로그(L2) | privacy | 중 | `console.error('[onError]', err)` 에러객체 원문 로깅 → DB 드라이버 에러에 입력PII 혼입 소지 | 값 마스킹 후 로깅 / 원문 | `solsol-brand-api/src/index.ts:233` | 바탕 | open |
| br01-r02-D15 | master 8테이블 | 데이터·스키마 | 중 | docs 정본(R1~R3+Q1Q2) 신규컬럼 15+(TB_USER 5·TB_PLAN 6·SUBSCRIPTION 1·INVOICE 1·PAYMENT 4·CONTACT 4·SITE 3)이 라이브 Aurora·ORM 미러 미적용(둘끼리는 동기) | docs↔라이브 동기 / docs만 앞섬 | docs `master.sql` vs `000_master.sql`·`schema.master.ts` | 바다·바탕 | **closed@migration**(신규 24컬럼+인덱스2 라이브 Aurora 적용·information_schema 검증. ORM 미러 schema.master.ts(brand-api·solsol-api)·000_master.sql 갱신. 회귀 스모크 무영향. ⚠️단 product 레포 커밋은 크로스세션 WIP 오염으로 총괄 판단 대기) |
| br01-r02-D16 | TB_CONTACT.contact_state | 데이터정합 | 하 | 값 의미론 드리프트(docs pending/in_progress/answered vs 라이브·seed open/answered/closed) | 05:894 3상태 / 구값 | docs `master.sql:372`·seed | 바다·바탕 | open |
| br01-r02-D17 | TB_USER 9502 | 데이터정합 | 하 | 탈퇴셀러 withdrawn_at 미설정(D15 컬럼부재) → 파기 기산점 없음 | D15 후 backfill | seed·`master.sql:58` | 바다 | open(D15 후) |
| br01-r02-D18 | TB_PAYMENT·CONTACT_REPLY | 성능 | 하 | 결제실패목록·내답변 조회 복합인덱스 부재 제안(트래픽 낮아 우선순위 낮음) | 인덱스 커버 | `000_master.sql` | 바다 | open(백로그) |

## 인시던트 (투명 기록)
- **INC-01**: privacy 검증 중 스크립트 오류로 시드 셀러(seller1) refresh JWT 원문이 에이전트 출력에 노출(즉시 로그아웃, 단 stateless라 만료 ≤14일까지 유효). **합성 테스트 계정·시드 데이터뿐, 실사용자 영향 없음(accepted-risk)**. 권고: 테스트계정 비번 교체 / JWT_SECRET·REFRESH_PEPPER 로테이션은 brand-api 영향 크므로 오너 판단. D09(세션 revoke 부재)가 실효성 재확인.

## GNB a11y 이관 blocker(br01-r01-D02) — **closed@r02**
클릭/키보드 토글·aria-haspopup/expanded/controls·Esc/바깥클릭 닫기 구현 확인(AppHeader.vue). 해소.

## 원장 신호(허브 `_ledger`)
- **D01·D02 closed@r02-fix**(커밋 `c4bcb63`·프로덕션 재검증 완료) → **현 스파이크 실동작 차단 blocker 0**.
- 잔여 open blocker(상): D03(advanced 플랜 시드 전 선행·현 스파이크 미차단, TB_PLAN 미시드), D13(실 탈퇴 오픈 전·프론트 미호출). → **스파이크 조건부 GO 충족**(핵심 기능 blocker 0). 나머지(커버리지 D04~07·GA조건 D09~11·드리프트 D15) 후속 라운드.

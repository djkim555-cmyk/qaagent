{
  "_comment": "Playwright MCP(브라우저 자동화) 설정 예시. 활성화하려면 이 파일을 .mcp.json 으로 복사한 뒤 Claude Code를 재시작하고 서버 사용을 승인하세요. (보안상 .mcp.json 직접 생성은 사용자 승인이 필요합니다)",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@0.0.78"
      ]
    }
  }
}
